<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Property Type Tree View -->
    <record id="view_property_type_tree" model="ir.ui.view">
        <field name="name">property.type.tree</field>
        <field name="model">property.type</field>
        <field name="active" eval="False"/>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="code"/>
                <field name="name"/>
                <field name="parent_id"/>
                <field name="product_category_id" optional="show"/>
                <field name="property_count"/>
                <field name="active" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Property Type Form View -->
    <record id="view_property_type_form" model="ir.ui.view">
        <field name="name">property.type.form</field>
        <field name="model">property.type</field>
        <field name="active" eval="False"/>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(property_asset_management.action_property_property)d"
                                type="action"
                                class="oe_stat_button"
                                icon="fa-building"
                                context="{'search_default_property_type_id': id}">
                            <field name="property_count" widget="statinfo" string="Properties"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Type Name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="parent_id"/>
                            <field name="product_category_id"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="active"/>
                            <field name="color" widget="color_picker"/>
                        </group>
                    </group>
                    <group>
                        <field name="description"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Property Type Search View -->
    <record id="view_property_type_search" model="ir.ui.view">
        <field name="name">property.type.search</field>
        <field name="model">property.type</field>
        <field name="active" eval="False"/>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="code"/>
                <field name="parent_id"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <group>
                    <filter string="Parent Type" name="parent" context="{'group_by': 'parent_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Property Type Action -->
    <record id="action_property_type" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">property.type</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first property type
            </p>
            <p>
                Property types help you categorize properties like buildings, meeting rooms, offices, etc.
            </p>
        </field>
    </record>
</odoo>
